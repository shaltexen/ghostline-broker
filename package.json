{
  "name": "ghostline-broker",
  "version": "1.0.0",
  "private": true,
  "description": "GHOSTLINE handshake broker (PeerJS PeerServer)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "express": "^4.19.2",
    "peer": "^1.0.2"
  }
}
